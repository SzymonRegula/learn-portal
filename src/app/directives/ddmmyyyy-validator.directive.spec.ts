import { DdmmyyyyValidatorDirective } from './ddmmyyyy-validator.directive';

describe('DdmmyyyyValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DdmmyyyyValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
